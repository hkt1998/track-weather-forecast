- TypeScript 严格模式 + bundler 解析 + JSX transform 为 react-jsx，所有 .ts/.tsx 统一按 ESNext 模块处理。
- ESLint 使用 Flat Config (`eslint.config.mjs`)，通过展开 `eslint-config-next` 的 core-web-vitals 与 typescript 预设来继承官方规则。
- 路径导入统一使用 `@/*` 别名映射到项目根，避免相对路径深层嵌套。